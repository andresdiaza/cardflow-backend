Instrucciones para configurar la variable de entorno con el archivo JSON de credenciales de Firebase (GOOGLE_APPLICATION_CREDENTIALS).

Requisitos del sistema (Java 21, Maven/Gradle).

Estructura general (explicando dónde va la lógica de los endpoints, la conexión a Firestore y el módulo ETL para procesar los Excel).

Comando para ejecutar la aplicación localmente.
